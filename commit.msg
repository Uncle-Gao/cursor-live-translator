🚀 Release v1.1.0: Anti-Black-Screen Pro Architecture 

✨ 项目级高危漏洞修复 (Anti-Black-Screen):
- [防御断言] 升级 `dictionary.json` 注入逻辑，新增 `(?=\s*(?![:=]))` 负向检查，防止破坏 JS 对象属性赋值。
- [碎片净化] 移除了 `fragments.json` 中导致全局作用域内语法崩溃的极短泛用词（如 All, Default），坚决隔离底层灾难污染。
- [安全墙] 显式放弃并用多行注释隔离对 `nls.messages.json` 和第三方插件 `extensions/*` 的暴力正则操作，保护 Cursor 原生基系统。

🛡️ 平台核心稳定性提升 (Core Resiliency):
- [引擎逃逸] 每次汉化/恢复后，跨平台自动抹除底层遗留的 `CachedData` 与 `Code Cache`，彻底解决因为新老 JS 层字节长度不一致导致的致命级别白屏崩溃。
- [Gatekeeper 黑客绕过] 针对 macOS 环境强行启动底层沙箱重置，内置静默运行 `xattr -cr` 与 `codesign` 进行临时重签进程，无缝突破苹果强制封杀修改版应用的 JIT 权限锁链。

♻️ 体验与备份架构重置 (UX & Backup Upgrades):
- [智能嗅探] 针对没有将 Cursor 放在系统默认盘符的玩家开发了循环交互重置模块，并且支持对于拷贝进终端的冗余路径自动执行字符串裁剪与后缀 `/resources/app` 的智能猜测下探纠错。
- [极客面板] 添加 `console.clear()`，无论经历多少步循环与报错返回，永远为用户呈现一个清爽无垢的 Dashboard 仪表工具态。
- [双向挂载备份锁] 大幅重构底层，利用系统注入技术动态获取应用级真实版本信息（如 2.6.21），然后生成类似 `*.[版本号].bak` 的双向锁定关联备份名称。从源头上阻止了系统自动更新后新版系统被“古代”备份代码无知覆写掉的致命代际断层事故。
- [孤儿清理巡航机] 新增了极高的代码内存防腐机制：在启动 Dashboard 时顺便释放针对硬盘全境的 `fs.readdir` 巡航探针。毫秒级跨代扫描并清理不属于当前版本的庞大旧时代历史备份垃圾。

📦 平台发版参数同步:
- 编译版本号与工程代码号向上双翻同步推至 `v1.1.0` 和官方推荐底座 `2.6.21`，取消会引发交叉宿主挂壁降格问题的实验性 `arm64` 打包。
